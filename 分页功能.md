# WHO'S YOUR MASTER - 产品与技术规划文档 (黑客松MVP最终版)

## 1. 核心架构与技术栈

本项目采用**链上+链下混合架构**，并通过模拟的“Virtual Protocol”AI Agent 工作流，展示智能化内容运营的未来。

*   **链上 (On-Chain) - 信任层 (Base Sepolia 测试网):**
    *   **核心资产:** `DoggoNFT.sol` (ERC-721)，负责狗狗的唯一身份和所有权。
    *   **价值结算:** `PrizePool.sol`，负责奖池资金管理与分配逻辑。
    *   **技术栈:** `Solidity`, `Hardhat`/`Foundry`, `OpenZeppelin`, `Viem`.
*   **链下 (Off-Chain) - 体验层 (React):**
    *   **核心交互:** `React` (Vite) 驱动的单页面应用。
    *   **钱包连接:** `RainbowKit`.
    *   **链上交互:** `Wagmi` Hooks.
    *   **UI:** `TailwindCSS`, `DaisyUI`.
*   **Virtual Protocol (模拟) - 智能层 (React):**
    *   **实现:** 在前端通过精心设计的 UI 和 `setTimeout` **模拟**多 Agent 协同工作流。
    *   **目的:** 展示 AI 如何赋能公益内容创作，解决行业痛点。

---
## 2. 当前项目进度 (已完成)

我们已经成功搭建了一个功能完整的、纯前端驱动的 MVP 应用框架。

*   **✅ 首页:** 完成了 AI 匹配的完整模拟流程，从摄像头开启到匹配结果展示。
*   **✅ 用户中心:**
    *   实现了“三合一”的标签页布局 (`UserDashboard`)。
    *   完成了“爱心人士”和“领养方”两种角色的个人主页 UI (`UserProfile`, `OrgProfile`)。
    *   完成了内容广场的帖子展示和**纯前端**的点赞/评论状态切换及积分模拟增减 (`ContentPlaza`)。
    *   完成了静态的“奖池与规则”页面 (`RulesAndPrizes`)。
*   **✅ 代码清理:** 移除了所有旧模板的冗余页面、组件和依赖。

---
## 3. 下一步行动计划 (Next Steps for MVP)

我们的核心任务是**用真实的链上交互替换掉现有的前端模拟**，并搭建出演示所需的关键模块。

### **任务一：搭建“上帝”后台 (Admin Page)**

这是我们 Demo Day 的“总控制台”，**优先级最高**。

1.  **创建新页面:** 在 `src/pages`下创建一个 `admin` 目录及 `AdminPage.jsx` 组件，并在 `Router.jsx` 中添加 `/admin` 路由。
2.  **构建UI:**
    *   **区域一：记录积分上链。** 包含 `Token ID` 和 `Points` 两个输入框，以及一个“记录积分”按钮。
    *   **区域二：触发奖金分配。** 包含一个独立的“分配奖池”按钮。
3.  **编写逻辑:**
    *   使用 Wagmi 的 `useWriteContract` Hook 分别准备好与 `PrizePool` 合约的 `recordMonthlyPoints` 和 `distributePrizes` 函数的交互。
    *   点击按钮时，调用对应的写合约函数，触发钱包签名。
    *   **技术栈:** `React`, `Wagmi`

### **任务二：实现 "Virtual Protocol" AI Agent 模拟**

在“领养方”的个人主页，将静态的 UI 升级为动态的、可演示的工作流。

1.  **创建工作台组件:** 在 `src/pages/userDashboard/orgProfile` 中创建一个新组件 `VirtualProtocolWorkbench.jsx`。
2.  **构建UI:** 包含一个“上传照片”的按钮和一个用于展示 Agent 工作流程的动态区域。
3.  **编写逻辑:**
    *   点击上传后，用 `setTimeout` 模拟 **Agent 1** 的分析过程，然后在 UI 上将图片重新排序和分组。
    *   点击“生成帖子”后，用 `setTimeout` 模拟 **Agent 2** 的写作过程，然后在文本框中填入预设好的文案。
    *   点击“发布”后，将生成的数据添加到 `ContentPlaza` 的帖子列表状态中。
4.  **整合:** 在 `OrgProfile.jsx` 中用这个新组件替换掉部分静态内容。
    *   **技术栈:** `React (useState, setTimeout)`, `CSS Animations`

### **任务三：让前端与“活水”相连 (Connect to Contracts)**

用真实的合约数据替换掉前端的硬编码模拟数据。

1.  **奖池总额:**
    *   在 `RulesAndPrizes.jsx` 中，使用 Wagmi 的 `useReadContract` Hook 读取 `PrizePool` 合约的奖池总额，并替换掉当前的静态数字。
2.  **我的狗狗 NFT:**
    *   在 `MyDoggoNFTs.jsx` 中，使用 Wagmi 的 `useReadContract` Hook 读取 `DoggoNFT` 合约，查询当前连接的钱包地址所拥有的 NFT 列表，并动态渲染。
3.  **透明化记录 (可选):**
    *   在 `RulesAndPrizes.jsx` 中，可以增加一个板块，使用 Wagmi 的 `useWatchContractEvent` Hook 监听 `PrizePool` 合约的 `DonationReceived` 和 `PrizeDistributed` 事件，并将它们展示出来。
    *   **技术栈:** `Wagmi`

---
## 4. 智能合约 (参考)

在执行任务一之前，需要先准备好以下两个合约并部署到 Base Sepolia。

*   **`DoggoNFT.sol` (ERC721):**
    *   `mint(to, tokenId)`: 只有管理员能调用的铸造函数。
    *   `ownerOf(tokenId)`:标准的查询持有者函数。
    *   `tokenURI(tokenId)`: 标准的元数据链接函数。
*   **`PrizePool.sol`:**
    *   `donate()`: `payable` 函数，接收捐款。
    *   `recordMonthlyPoints(tokenIds, points)`: 只有管理员能调用的记录积分函数。
    *   `distributePrizes()`: 任何人都可以调用的分配函数。
    *   `totalPrizePool()`: `view` 函数，返回当前奖池总额。
    *   `event DonationReceived(from, amount)`
    *   `event PrizeDistributed(to, tokenId, amount)`

